[package]
name = "qv-cluster"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Ring placement, Raft metadata state machine, quorum replication, hinted handoff, read repair, and shard transfer for quorvec (M3+)."

[dependencies]
xxhash-rust = { workspace = true }

[dev-dependencies]
